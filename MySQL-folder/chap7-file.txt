-- CONDITIONALS
-- TRY SOME NUMERIC DATA LIKE UNIT PRICE
-- LET'S CHOOSE SOME CHEAP AND AFFORDABLE ITEMS

-- LEVEL 1
SELECT 
	*,
    CASE 
    WHEN unit_price <= 100 THEN 'AFFORDABLE'
    WHEN unit_price <= 200 THEN 'NORMAL'
    ELSE 'EXPENSIVE (BUT NOT FOR YOU!)'
    END AS PRICE_CATEGORY
FROM 
	dim_product;
    

-- LEVEL 2
-- MAKE YOU BECOME A PRO DEVELOPER!!!!
SELECT 
	*,
    CASE 
    WHEN unit_price <= 100 AND category = 'CLOTHING' THEN 'AFFORDABLE'
    WHEN unit_price <= 200 AND category = 'CLOTHING' THEN 'NORMAL'
    WHEN unit_price > 200 AND category = 'CLOTHING' THEN 'EXPENSIVE (BUT NOT FOR YOU!)'
    ELSE concat('NOT FOR ',category)
    END AS PRICE_CATEGORY
FROM 
	dim_product;

-- WINDOW FUNTIONS NOT LIKE AGGREGATE FUNTIONS
-- TO DO RANKING, RUNNING TOTALS, MOVING AVERAGES, COMPARIRIONS BETWEEN ROWS
-- KEEPING ALL THE ROWS IN THE RESULT
-- CHAPTER 8
