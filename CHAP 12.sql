-- VIEWS

CREATE VIEW DEDUP_VIEW AS
SELECT
	SUBQUERY.*
FROM
(
SELECT 
	*,
    ROW_NUMBER() OVER(PARTITION BY ID ORDER BY ID) AS DEDUP
FROM
	CUSTOMERS
) SUBQUERY
WHERE
	DEDUP = 1;

SElECT * FROM dedup_view